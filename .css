.deva-wnd {
    position: fixed;
    top: 10%;
    left: 50%;
    transform: translateX(-50%);
    background: linear-gradient(145deg, #1e1e2f, #252538);
    color: #fff;
    border: 1px solid #444;
    border-radius: 6px;
    width: 600px;
    z-index: 10000;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
    overflow: hidden;
}
.deva-header {
    background: #111;
    color: #eee;
    padding: 10px 15px;
    text-align: center;
    border-bottom: 2px solid #444;
    font-size: 1.1em;
    letter-spacing: 1px;
}
.deva-tabs {
    display: flex;
    justify-content: center;
    margin-top: 5px;
}
.deva-tab {
    background: #333;
    color: #eee;
    border: 1px solid #555;
    padding: 5px 10px;
    margin: 0 5px;
    border-radius: 6px;
    cursor: pointer;
    transition: background 0.3s, transform 0.2s;
}
.deva-tab:hover {
    background: #444;
    transform: scale(1.05);
}
.deva-tab.active {
    background: #555;
    color: #fff;
    border: 1px solid #666;
}
.deva-content {
    padding: 15px;
    height: 200px;
    overflow-y: scroll; /* Ensures scrolling is possible */
    background: #202030;
    border-top: 2px solid #444;
    border-bottom: 2px solid #444;
    display: flex;
    flex-direction: column;
}
.deva-tab-content.hidden {
    display: none;
}
.deva-addon {
    padding: 10px 0;
}
.deva-addon h3 {
    color: #f0f0f0;
    margin: 0;
}
.deva-addon:last-child {
    border: none;
}
.deva-footer {
    background: #111;
    color: #ddd;
    text-align: center;
    padding: 10px;
    font-size: 8px;
    border-top: 2px solid #444;
}
.deva-footer a {
    color: #4da6ff;
    text-decoration: none;
    transition: color 0.3s;
}
.deva-footer a:hover {
    color: #88ccff;
}
.deva-log {
    margin-bottom: 15px;
}
.deva-log h4 {
    color: #ccc;
}
.deva-log hr, .addon-separator {
    border: 2px dashed #444;
    margin: 10px 0;
}
.deva-dock {
    background: url(https://i.imgur.com/Y6z86Ud.gif);
    width: 32px;
    height: 32px;
    position: absolute;
    top: -35px;
    left: 73px;
    cursor: pointer;
}
.unblock-scroll {
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: rgba(0, 0, 0, 0.3) rgba(100, 100, 100, 0.5); /* Custom scrollbar colors */
}
.unblock-scroll::-webkit-scrollbar {
    width: 8px;
}
.unblock-scroll::-webkit-scrollbar-thumb {
    background: #0003;
    border-radius: 4px;
}
.unblock-scroll::-webkit-scrollbar-track {
    background: gray;
    border: 1px solid gray;
}
.deva-close-button {
    position: absolute;
    top: 10px;
    right: 10px;
    cursor: pointer;
    background: url(../img/gui/buttony.png?v=1737470974199) -624px -117px;
    width: 12px;
    height: 12px;
}
.deva-close-button:hover {
    background-position: -726px -117px;
}
